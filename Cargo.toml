[package]
name = "uuid-lite"
version = "1.10.0"
edition = "2024"
description = "A minimal random (v4) UUID generator."
license = "MIT"

[lib]
name = "uuid_lite"
path = "src/lib.rs"

[dependencies]
getrandom = "0.4"
